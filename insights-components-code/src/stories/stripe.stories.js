import React from 'react'
import { StripeCheckoutButton } from '../components/StripeButton/stripe'

export default {
  title: 'Stripe',
  component: StripeCheckoutButton
}

export const DefaultStripeButton = () => <StripeCheckoutButton />
